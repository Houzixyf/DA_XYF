def f(x, u, par, evalconstr=True):
    k = par[0]
    x1, x2, x3, x4 = x  # state variables
    u1, = u  # input variable

    e = 0.9  # inertia coupling

    s = sin(x3)
    c = cos(x3)

    ff = [x2,
                   u1,
                   x4,
                   -e * x2 ** 2 * s - (1 + e * c) * u1
                   ]

    if evalconstr:
        res = 0* pe(k, 0.1, 10) #  pe(k, 0, 10)
        ff.append(res)

    return ff